---
import BlogListingPage from '../../../components/BlogListingPage.astro';
import { getBlogListingStaticPaths } from '../../../utils/blog';

export async function getStaticPaths() {
	return getBlogListingStaticPaths();
}

const currentPage = Number(Astro.props.page);
---

<BlogListingPage currentPage={currentPage} basePath="/blog" />
